<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Crud.MainPage" BackgroundColor="#2bcbff">

    <ContentPage.Content>

        <StackLayout>
            <Entry x:Name="Name" Placeholder="Name"></Entry>
            <Entry x:Name="address" Placeholder="address"></Entry>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                <Button x:Name="SaveButton" Text="Save Data" Clicked="SaveButton_Clicked"></Button>
                <Button x:Name="ShowButton" Text="Show Data" Clicked="ShowButton_Clicked"></Button>
            </StackLayout>
            <ListView x:Name="Datalist">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" Detail="{Binding Address}"></TextCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
                
            </ListView>
            
        </StackLayout>        
    </ContentPage.Content>

</ContentPage>
